# Render에 배포된 FastAPI 백엔드 URL (trailing slash 없이)
# Vercel 배포 시: Environment Variables에 아래 값 설정
VITE_API_URL=https://personal-news-be43.onrender.com

# 로컬 개발 시: 빈 문자열로 두면 Vite 프록시(/api → localhost:8000)가 처리
# VITE_API_URL=
